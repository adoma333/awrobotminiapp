تعليمات دائمة لوكيل الذكاء الاصطناعي — AW Robot
هذا الملف يُقرأ تلقائياً من قبل Gemini CLI في بداية كل مهمة (يجب أن يبقى اسمه GEMINI.md في جذر الريبو). لا تحذفه.
سياق المشروع
الاسم: AW Robot — بوت تلجرام (@awfxapp_bot) + Mini App لربط حساب MT5 وعرض الرصيد والنمو ونسبة الربح لحظياً.
الستاك: FastAPI (خلفية) + Firestore (قاعدة بيانات) + React/Vite (واجهة المستخدم) + React/Vite منفصل للوحة تحكم الأدمن على /admin/ + nginx + systemd + MT5 عبر Wine على نفس سيرفر Ubuntu VPS.
الدفع: NOWPayments (USDT/TON) و Telegram Stars ومحفظة TON Connect.
هذا الريبو نسخة تطوير فقط، غير متصل بسيرفر الإنتاج مباشرة.
قواعد عمل إلزامية
افحص الكود الفعلي أولاً. لا تفترض أسماء ملفات أو مسارات أو مكتبات غير موجودة — ابنِ على ما هو موجود بالفعل في الريبو.
نفّذ المهمة المطلوبة في الـ Issue فقط. لا توسّع النطاق لتعديلات لم تُطلب صراحة، حتى لو بدت "تحسيناً منطقياً".
لا تحذف أو تعدّل منطق الدفع أو التداول الآلي الحالي إلا إذا كانت المهمة المطلوبة تحديداً تخص هذا الجزء — وفي هذه الحالة وثّق كل تغيير بوضوح شديد في رسالة الـ commit.
لا تُدرج أي مفاتيح API، توكنات، أو أسرار في الكود. استخدم متغيرات البيئة الموجودة أو أضف مثالاً في .env.example فقط.
اكتب كوداً يتوافق مع الأنماط الموجودة في الملفات المجاورة (تسمية المتغيرات، شكل الـ endpoints، أسلوب معالجة الأخطاء) بدل فرض أسلوب جديد.
قبل أي تعديل جوهري في منطق حساس (المدفوعات، صلاحيات المستخدم، auto_trading_enabled)، اشرح في رسالة الـ commit بالتفصيل لماذا التعديل آمن.
لا تدمج (merge) أي فرع بنفسك. مهمتك تنتهي بفتح Pull Request فقط — المراجعة والدمج تتم يدوياً من صاحب المشروع.
إذا واجهت غموضاً حقيقياً يمنع التنفيذ الصحيح (مثال: الملف المذكور غير موجود، أو المطلوب يتعارض مع كود موجود)، وضّح ذلك بوضوح في رسالة الـ commit وفي وصف التعديلات، ونفّذ أفضل تفسير معقول بدل التوقف الكامل.
نطاق الصلاحيات
هذا الوكيل يعمل حصراً داخل هذا الريبو (نسخة التطوير). لا اتصال مباشر بسيرفر VPS الإنتاج، ولا وصول لقاعدة بيانات Firestore الحقيقية، ولا مفاتيح دفع حقيقية. كل تعديل ينتج Pull Request يحتاج موافقة بشرية قبل أي أثر فعلي.
